#pragma once

#include <QCoreApplication>
#include <QTime>

/**
 * @brief delay function which is imitation of time sleep
 * @param delay_miliseconds number of miliseconds while function has to sleep
 */
void delay(int delay_miliseconds);
